# Docker Compose environment variables
# Copy this file to .env and adjust as needed:
#   cp .env.example .env
#
# Also ensure backend .env exists for backend-specific settings:
#   cp import-machine-backend/env.example import-machine-backend/.env
# Then update import-machine-backend/.env with DB_USER and DB_PASSWORD matching below.

# Database (used by PostgreSQL, backend)
DB_USER=importmachine
DB_PASSWORD=importmachine_secret
DB_NAME=import-machine
DB_PORT=5432

# Backend
BACKEND_PORT=5000
NODE_ENV=development
JWT_SECRET=your_super_secret_jwt_key_change_in_production
# For LAN access, set to your LAN IP (e.g. http://192.168.1.100:3000)
FRONTEND_URL=http://localhost:3000

# Frontend
FRONTEND_PORT=3000
# Empty = use relative URLs via nginx proxy (recommended for Docker). Set to http://localhost:5000 for direct backend URL.
REACT_APP_BACKEND_URL=
