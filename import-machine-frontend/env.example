# Frontend Environment Configuration

# Backend API URL
# - Empty = use relative URLs (same-origin; nginx proxies /api to backend) - used in Docker
# - http://localhost:5000 = direct backend URL - used for local dev without Docker
REACT_APP_BACKEND_URL=http://localhost:5000

# Other React environment variables can be added here
# All variables must be prefixed with REACT_APP_
